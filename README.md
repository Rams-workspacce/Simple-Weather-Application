The Simple Weather Application consists three files, "index.html", "styles.css", "script.js" .
As the name says, it is a simple weather application.
Just download the files and save it in your local repository.
Then simply, execute the code.
After executing, you need to enter the city name for getting the weather details of that city.
